import React from 'react';

function Nav() {
    const {
        // insert categorical data
    } = props;

    return (
        <header>
            <h1>
                {/* insert heading */}
            </h1>
            <nav> 
                {/* insert nav  */}
            </nav>
        </header>
    );
}

export default Nav;